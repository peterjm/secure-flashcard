<% set_page_title "Edit: #{@card.question}" %>

<% content_for :side_menu do %>
  <%= ui_side_menu_link('Delete this card', card_path(@card), method: :delete, confirm: "Delete card?") %>
<% end %>

<%= render 'form' %>
